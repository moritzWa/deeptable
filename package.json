{
  "name": "deeptable-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "start": "bun --filter '*' start"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "concurrently": "^9.1.2"
  }
}
